#coding=utf-8

import os.path

def CheckStringSuffix(tempStr,suffixSet):
    for suffix in suffixSet:
        if tempStr.endswith(suffix):
            return true
    return False

def main():
    codeFile
    for root,dirs,files in os.walk(ui_proj_abs_path):
        for file in files:
            if file.endswith(".cpp"):
            
            
